import React from 'react';
import {withRouter} from 'react-router-dom';
import ModelsBio from '../../components/ModelsBio';

function models_bio () {  
  return (
     <>
     <ModelsBio/>
     </>  
     );
 };
export default withRouter(models_bio);